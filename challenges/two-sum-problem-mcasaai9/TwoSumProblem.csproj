<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <StartupObject>TestRunner</StartupObject>
    <AssemblyName>TwoSumProblem</AssemblyName>
    <RootNamespace>TwoSumProblem</RootNamespace>
    
    <LangVersion>latest</LangVersion>
    <UseAppHost>true</UseAppHost>
  </PropertyGroup>

  <ItemGroup>
    <!-- 🚫 Solution-Dateien ausschließen -->
    <Compile Remove="solution.cs" />
    <Compile Remove="**/*Solution*.cs" />
    
    <!-- 📖 Als Referenz behalten -->
    <None Include="solution.cs" Condition="Exists('solution.cs')" />
  </ItemGroup>

  <!-- 📖 Build-Info -->
  <Target Name="ShowBuildInfo" BeforeTargets="Build">
    <Message Text="🚀 .NET 9.0 | Challenge: Two Sum Problem" Importance="normal" />
    <Message Text="📖 solution.cs als Referenz (ignoriert)" 
             Condition="Exists('solution.cs')" 
             Importance="normal" />
  </Target>

</Project>